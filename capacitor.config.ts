import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.makkahchamber.committees',
  appName: 'MakkahChamber',
  webDir: 'dist'
};

export default config;
