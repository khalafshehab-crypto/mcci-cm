import re

with open('src/pages/CommitteesLibrary.tsx', 'r') as f:
    content = f.read()

old_block = '''      let systemPrompt = "";
      if (aiGenMode === "new") {'''

new_block = '''      let systemPrompt = "";
      if (workspaceService === "circular") {
        systemPrompt = `أنت مساعد إداري ذكي في الغرفة التجارية (غرفة مكة المكرمة).
الهدف: تحليل التعميم أو الخطاب المرفق، واستخراج البيانات الأساسية منه، وصياغة نص توجيهي مختصر لإرساله للجان.

لقد تم إرفاق ملف التعميم (صورة أو PDF)، يرجى قراءته بدقة واستخراج التالي:
1. "التعميم وارد من": (اسم الجهة المصدرة للتعميم)
2. "رقم وتاريخ": (رقم وتاريخ التعميم المذكور فيه، إن وجد، وإلا اكتب غير محدد)
3. "الموضوع": (موضوع التعميم)
4. "عرض التعميم": (قم بصياغة نص توجيهي إداري رصين ومختصر، بناءً على محتوى التعميم، يتم توجيهه لأعضاء اللجان للإحاطة والعمل بموجبه. لا تكتب ديباجة كاملة، فقط النص التوجيهي.)

يجب أن يكون الرد منسقاً بالشكل التالي تماماً بدون أي إضافات:
التعميم وارد من: [النص المستخرج]
رقم وتاريخ: [النص المستخرج]
الموضوع: [النص المستخرج]
عرض التعميم:
[نص التوجيه والصياغة هنا]`;
      } else if (aiGenMode === "new") {'''

content = content.replace(old_block, new_block)

with open('src/pages/CommitteesLibrary.tsx', 'w') as f:
    f.write(content)
